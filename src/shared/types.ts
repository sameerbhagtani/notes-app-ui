export type Theme = {
    background: string;
    card: string;
    text: string;
    secondaryText: string;
    accent: string;
    danger: string;
};

export type ThemeMode = "system" | "light" | "dark";
